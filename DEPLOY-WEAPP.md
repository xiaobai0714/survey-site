# 0 成本微信小程序部署说明（个人主体 + 微信云开发）

本目录 `weapp/` 是一个**匿名意见收集小程序**，配合**微信云开发（CloudBase）**作为后端，
**全程 0 元**：不需要买服务器、不需要备案域名、数据存在腾讯云永久保存。

> 适合：想让别人在微信里直接填意见、你登录管理页看每条意见（时间 + 内容）。
> 不适合：需要微信支付、需要收集用户手机号/真实身份（个人主体不支持，且违背"匿名"）。

---

## 一、你本人要做的事（必须，我代不了）

### 1. 注册小程序账号（免费，个人）
1. 手机/电脑打开 https://mp.weixin.qq.com
2. 点右上角「立即注册」→ 选「小程序」
3. 用你的**微信号**扫码，按提示完成**个人**主体注册（无需认证、不花钱）
4. 注册后在「开发 → 开发管理 → 开发设置」里找到 **AppID（小程序ID）**，复制下来

### 2. 下载微信开发者工具（免费）
- https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html
- 选「稳定版」对应你系统的版本安装

### 3. 导入项目
1. 打开微信开发者工具 → 扫码登录（用同一个微信号）
2. 点「导入项目」
3. 目录选本仓库里的 `weapp/` 文件夹
4. AppID 填你刚复制的（不要留 touristappid）
5. 点「导入」

### 4. 开通云开发（免费额度）
1. 工具顶部点「云开发」→ 「开通」
2. 按提示创建环境（环境名称随便取，如 `opinion-prod`）
3. 创建好后，**记下环境 ID**（形如 `opinion-prod-xxxx`）
4. 打开 `weapp/miniprogram/app.js`，把 `env: 'YOUR_ENV_ID'` 改成你的环境 ID

### 5. 建数据库集合
1. 云开发控制台 → 「数据库」→ 「新建集合」
2. 集合名填 **`opinions`**（必须完全一致）
3. 权限设为「所有用户可读，仅创建者可读写」或「仅管理端可写」（匿名提交用云函数写，建议选「仅创建者可读写」即可，因为写入走云函数服务端）

### 6. 部署云函数
1. 在工具左侧文件树，右键 `cloudfunctions/submitOpinion` → 「上传并部署：云端安装依赖」
2. 同样右键 `cloudfunctions/listOpinions` → 「上传并部署：云端安装依赖」
3. 两个都显示「上传成功」即可

### 7. 预览 / 真机调试
- 工具里点「编译」可在模拟器看效果
- 点「预览」用手机微信扫码，真机填写测试一条
- 进管理页：把页面路径临时改成 `pages/admin/admin`（开发者工具里「编译模式」可加），或扫码后手动进入，密码 `123456789`

### 8. 提交审核发布（免费）
1. 工具里点「上传」→ 填版本号（如 1.0.0）和备注 → 上传
2. 去 mp.weixin.qq.com → 「版本管理」→ 提交审核
3. **类目选择**：建议选「工具-信息查询」或「其他」，功能描述写「用户匿名意见/建议收集」
   - ⚠️ 不要选「问卷/调查」类目（个人主体可能要《增值电信业务经营许可证》等资质，易驳回）
4. 审核通常 1–2 天，通过后点「发布」即可

发布后别人在微信里搜到你小程序、或扫小程序码就能填意见；你用同一小程序进管理页（密码 `123456789`）查看所有意见。

---

## 二、费用说明（为什么是 0 成本）
- 小程序账号（个人）：**免费**
- 微信云开发基础配额：**免费**（数据库 2GB、云函数/请求有免费额度，小流量永远用不完）
- 审核、发布：**免费**
- 唯一可能产生费用的情况：单月请求/存储**远超免费额度**（普通意见收集几乎不可能），超了才按量计费

## 三、想改内容？
- **改管理密码**：编辑 `weapp/cloudfunctions/listOpinions/index.js` 里的 `ADMIN_PWD`，重新上传该云函数
- **改提示文案 / 配色**：编辑对应页面的 `.wxml` / `.wxss`，重新「上传」版本并发布
- **限制字数**：`index.wxml` 的 `maxlength` 和 `submitOpinion/index.js` 的 `.slice(0, 500)`

## 四、与现有网站的关系
- 网站版（`server.js` + ngrok / 服务器）和这个小程序是**两套独立存储**，数据不互通
- 小程序走云开发数据库，长期 0 成本、不用本机开着，更适合长期使用
- 旧网站可作为备用；两者可并存
